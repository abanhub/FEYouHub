YOUTUBE_API_KEY=AIzaSyBAyYCNA0AiFV_QsiDWxMTPxV-wH1SR0a8

# Backend (Node/Express)
# API server port (server/index.js)
API_PORT=2006
# Comma-separated origins for CORS. Use * to allow all in dev.
CORS_ORIGIN=*
# Default region for /api/trending when client doesn't specify
DEFAULT_TRENDING_REGION=VN

# Frontend dev server (Vite)
DEV_HOST=::
DEV_PORT=8089
# Where the dev proxy sends /api requests
API_HOST=huhu

# Frontend: base address for youtube proxy used by the SPA
VITE_API_ORIGIN= https://youtubei-proxy.bangngo1509a.workers.dev
VITE_API_BASE_PATH=/youtubei/v1
# Optional: Invidious instance for future use

# Frontend test mode: show skeletons in grids
VITE_TEST_SKELETON=0
